"use strict";


function goMap () {
  if ($('#map').length) {
        // Styles a map in night mode.
        
		// To add the marker to the map 

  };
};


// Dom Ready Function
jQuery(document).on('ready', function () {
	(function ($) {
		// add your functions
		goMap()
	})(jQuery);
});